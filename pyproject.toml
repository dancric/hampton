[project]
name = "hampton"
version = "0.1.0"
description = "Riskgaming simulation engine"
requires-python = ">=3.10"
dependencies = [
    "openai",
    "pydantic",
    "tiktoken"
]

[project.optional-dependencies]
dev = [
    "ipython",
    "pytest",
    "ipdb",
    "ruff",
]